# Data-Retriever

# ETL-пайплайн для сбора метрик рекламных креативов

Простой, но полнофункциональный **ETL-проект**, который автоматизирует сбор данных о производительности рекламных роликов из разрозненных источников (Google Drive и Google Sheets) и загружает их в централизованную облачную SQL-базу данных.

## 🚀 Проблема

На моей текущей работе данные о производительности креативов хранятся в двух разных местах:

1.  **Метрики Hook/Hold:** Периодически выгружаются в виде скриншотов (изображений с текстом) в папку Google Drive.
2.  **Метрики тестов (CTR, CR, Installs и т.п.):**  Автоматически публикуются в общей Google-таблице.

Мне необходимо периодически сверяться с показателями Hook/Hold креативов. Для этого необходимо было каждый раз заходить в гугл диск в соответствующую папку и проверять не появились ли новые данные-картинки, а затем открывать по-отдельности изображения каждого отдельного ролика и подмечать ролики с высокими показателями. Одновременно с этим мне также полезно иметь в виду и остальные метрики таких роликов, которые хранятся в таблице тестов. 

Я решил автоматизировать этот процесс. К сожалению в виду ограничений доступа я не мог воспользоваться Google API для полной автоматизации и решил сделать полуавтоматический процесс: создать скрипт в гугл-ноутбуках, что будет считывать с помощью OCR информацию с однотипных картинок и выгружать нужные мне метрики с названиями роликов в личную таблицу. Также я создал скрипт, что копирует данные из таблицы с тестами, чтобы вся информация в итоге хранилась у меня в одном месте.

Для удобства я сделал также простой google script, который раз в сутки проверяет появились ли новые данные в папке с картинками и уведомляет меня в телеграме с помощью чат-бота.

Для возможности дальнейшей демонстрации, я создал абсолютно фейковые данные (таблица тестов и папки с картинками), аналогичные данным с моей работы, на моем личном гугл-аккаунте и решил сделать загрузку данных в SQL базу данных.  

## 🛠️ Решение: Архитектура

В итоге я реализовал два независимых ETL-пайплайна, которые выполняются в среде Google Colab и загружают данные в **единый источник правды** (Single Source of Truth) — облачную базу данных **PostgreSQL**, размещенную на **Neon**.



### Пайплайн 1: OCR (Из Google Drive)

* **Extract:** Скрипт сканирует папки Google Drive (примонтированные к Colab), отслеживая уже обработанные даты.
* **Transform:**
    1.  Изображения обрезаются до нужной области (`cv2`).
    2.  Текст распознается с помощью **EasyOCR**.
    3.  Имена проектов и креативов парсятся из имени файла.
    4.  Метрики `Hook` и `Hold` извлекаются из текста с помощью `regex`.
* **Load:** Данные загружаются в таблицу `hook_hold_metrics` в PostgreSQL с использованием `UPSERT` (для идемпотентности).

### Пайплайн 2: GSheets (Из Google-таблиц)

* **Extract:** Скрипт подключается к Google Sheets API (через Service Account), **автоматически определяет имя проекта** по названию листа и запрашивает у SQL-базы последнюю загруженную дату.
* **Transform:**
    1.  Загружаются только *новые* строки (по дате).
    2.  Данные проходят валидацию и очистку (например, `"-"` в `bench` заменяется на `NULL`).
    3.  Строки приводятся к правильным типам данных (`Integer`, `Float`).
* **Load:** Данные загружаются в таблицу `auto_test_metrics` с использованием `UPSERT` по составному ключу (`video_id`, `date`, `team`).

---

## 💻 Технологии

* **Язык:** Python
* **Среда выполнения:** Google Colab
* **База данных:** **PostgreSQL** (в облаке **Neon**)
* **ETL-библиотеки:** **SQLAlchemy** (для ORM и безопасных `UPSERT`-ов), `gspread` (для Google Sheets API), `opencv-python` (для обработки изображений), `EasyOCR` (для распознавания текста).
* **Ключевые навыки:** Проектирование ETL-пайплайнов, **моделирование данных** (Data Modeling), работа с облачными БД, `UPSERT`-логика.

---

## 📂 Структура репозитория
